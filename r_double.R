value <- 42

# load_data <- function(input) {
#   value <<- input$value
#   return(TRUE)
# }

# process <- function(input) {
#   if (is.null(value)) {
#     return(list(error = "missing field 'value'"))
#   }
#   doubled <- 2 * value
#   cat("Processing input:", value, "->", doubled, "\n")
#   list(value = doubled)
# }
